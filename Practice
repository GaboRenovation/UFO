// 11.1.3

// Printing a string with JavaScript
console.log("Hello, world!");


// LISTAS Y FUNCIONES
let friends = ["Sarah", "Greg", "Cindy", "Jeff"];

function listLoop(userList) {
   for (var i = 0; i < userList.length; i++) {
     console.log(userList[i]);
   }
}

console.log(listLoop(friends));




// CLEAR DATA BEFORE CODING
// it is a best practice Because we need to clear the data first, 
// otherwise the data users search will already be filtered when they search again. 

tbody.html("");.

